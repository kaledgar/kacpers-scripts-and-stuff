# Notes

```
python3 -m venv env
source env/bin/activate

pip install django
django-admin startproject project

cd project/
python3 manage.py runserver <port> # 8000 default

python3 manage.py startapp core
```